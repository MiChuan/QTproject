#include "supplier.h"

Supplier::Supplier(const QString &supplierName,const QString &fruitName, const double &price, const double &number,
                   const QString &address, const QString &telephone)
    :supplierName(supplierName), fruitName(fruitName),price(price),number(number), address(address),telephone(telephone)
{

}

